type Student {
  id: ID!
  name: String!
}

type Task {
  id: ID!
  title: String!
  description: String!
  answers: [TaskAnswer!]
}

type TaskAnswer {
  id: ID!
  content: String!
  timestamp: String!
}

type Query {
  students: [Student!]!
  tasks: [Task!]!
}

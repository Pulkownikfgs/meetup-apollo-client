overwrite: true
schema: 'src/schema.graphql'
documents: null
generates:
  src/generated/graphql.ts:
    plugins:
      - 'typescript'
      - 'typescript-resolvers'
  src/graphql.schema.json:
    plugins:
      - 'introspection'
